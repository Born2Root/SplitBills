package org.weilbach.splitbills.util

/*
import org.weilbach.splitbills.Amount

class AmountContainer {

    private val itemsMap: LinkedHashMap<String, Amount> = LinkedHashMap()

    val items = itemsMap.values

    val size: Int
        get() = itemsMap.size

    fun contains(item: Amount) = itemsMap.contains(item.id)

    fun add(item: Amount): Boolean {
        if (contains(item)) {
            return false
        }
        itemsMap[item.id] = item
        return true
    }

    fun get(id: String) = itemsMap[id]

    fun remove(id: String) = itemsMap.remove(id)

    fun forEach(operation: (Amount) -> Unit) {
        itemsMap.forEach {
            operation(it.value)
        }
    }
}*/
