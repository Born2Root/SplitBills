package org.weilbach.splitbills

import org.weilbach.splitbills.data2.Member

/*
interface GroupMergeCallback {

    fun success(group: Group)

    fun error(group: Group)

    fun memberAdded(group: Group, member: Member)

    fun billAdded(group: Group, bill: Bill)

    fun billRemoved(group: Group, bill: Bill)
}*/
