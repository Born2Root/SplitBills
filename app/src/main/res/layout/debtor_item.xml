<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View.OnClickListener" />

        <import type="android.view.View" />

        <variable
            name="viewmodel"
            type="org.weilbach.splitbills.addeditbill.DebtorItemViewModel" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/debtor_item_linear_layout_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_alignParentStart="true">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/activity_vertical_margin"
                android:layout_marginTop="@dimen/list_item_padding"
                android:layout_marginEnd="@dimen/activity_vertical_margin"
                android:text="@{viewmodel.name}"
                android:textAppearance="@style/TextAppearance.AppCompat.Body2" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/activity_vertical_margin"
                android:layout_marginEnd="@dimen/activity_vertical_margin"
                android:layout_marginBottom="@dimen/list_item_padding"
                android:text="@{viewmodel.email}" />

        </LinearLayout>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_toEndOf="@+id/debtor_item_linear_layout_name"
            android:onClick="@{() -> viewmodel.changeAmount()}"
            android:text="@{viewmodel.amountPretty}"
            android:visibility="@{viewmodel.isAmountValid ? View.VISIBLE : View.GONE}" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_alignParentEnd="true"
            android:onClick="@{() -> viewmodel.remove()}"
            android:text="@string/remove" />

    </RelativeLayout>
</layout><!--
    <?xml version="1.0" encoding="utf-8"?>
    <layout xmlns:android="http://schemas.android.com/apk/res/android">

        <data>

            <import type="android.view.View.OnClickListener" />
            <import type="android.view.View" />

            <variable
                name="viewmodel"
                type="org.weilbach.splitbills.addeditbill.DebtorItemViewModelViewModel" />

            &lt;!&ndash;<variable
                name="navigator"
                type="org.weilbach.splitbills.MemberItemNavigator" /> &ndash;&gt;

        </data>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content" >

            <LinearLayout
                android:id="@+id/member_with_amount_item_linear_layout_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_alignParentStart="true">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/activity_vertical_margin"
                    android:layout_marginTop="@dimen/list_item_padding"
                    android:layout_marginEnd="@dimen/activity_vertical_margin"
                    android:text="@{viewmodel.name}"
                    android:textAppearance="@style/TextAppearance.AppCompat.Body2" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/activity_vertical_margin"
                    android:layout_marginEnd="@dimen/activity_vertical_margin"
                    android:layout_marginBottom="@dimen/list_item_padding"
                    android:text="@{viewmodel.email}" />

            </LinearLayout>

            <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_toEndOf="@+id/member_with_amount_item_linear_layout_name"
                android:onClick="@{() -> viewmodel.onAmountButtonClicked()}"
                android:text="@{viewmodel.amountWithCurrency}"
                android:visibility="@{viewmodel.amountButtonValid ? View.VISIBLE : View.GONE}"/>

            <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_alignParentEnd="true"
                android:onClick="@{() -> viewmodel.onRemoveButtonClicked()}"
                android:text="@string/remove"/>

        </RelativeLayout>
    </layout>-->